# `components/` — React UI

| Folder | Contents |
|--------|----------|
| **`auth/`** | Login, signup, password flows, sign out |
| **`dashboard/`** | Dashboard charts, stats, AI insights, demo controls |
| **`marketing/`** | Landing / progressive blur (home) |
| **`ui/`** | Buttons, cards, inputs, page headers, primitives |
| **`voice/`** | TTS, STT hooks, dashboard voice assistant |

Shared pieces at the root: **`AppNav`**, **`BackToHome`**, **`MedicalDisclaimer`**, **`SignedOutNotice`**.
