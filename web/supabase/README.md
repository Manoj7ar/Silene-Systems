# ElderCare database (reference)

SQL migrations live in the **repository root**: [`../../supabase/migrations`](../../supabase/migrations).

Use the Supabase CLI from the repo root (`npm run sb:start`, `npm run sb:reset`) so those files apply to local or linked projects.
